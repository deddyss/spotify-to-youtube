import chalk from 'chalk';

const greeting = `Before proceeding, make sure you are logged into Youtube on ${chalk.bold.yellow('Chrome')} and let the browser ${chalk.bold.yellow('remain open')}\n`
	+ `${chalk.bold('='.repeat(98))}`;

export default greeting;
