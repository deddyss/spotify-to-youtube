import chalk from 'chalk';

const greeting = `Before proceeding, make sure you are logged into Youtube on the ${chalk.bold.yellow('Chrome')} browser\n`
	+ `${chalk.bold('='.repeat(78))}`;

export default greeting;
